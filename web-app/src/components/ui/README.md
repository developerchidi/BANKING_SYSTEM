# UI Components

Thư mục này chứa các UI element tái sử dụng (Button, Input, Modal, Card, ...), dùng chung cho toàn bộ web-app.

- Đặt tên component theo PascalCase.
- Mỗi component nên có file riêng và export qua index.ts.
- Ưu tiên dùng Material-UI, có thể custom theo banking theme. 